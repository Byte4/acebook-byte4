$("#all-comments-<%=@post.id%>").append("<%=j render 'comments/comment', post: @post, comment: @comment %>");
$("#comment_content_<%= @post.id %>").val('')
$("#comment-count-<%= @post.id %>").text("(<%=j @post.comments.count.to_s %>)")
