<% @posts.each do |post| %>

  <div class="card">
    <div class="card-body">
      <%= simple_format(post.message) %>
    </div>
  </div>

<% end %>

<%= link_to new_post_path do %>
New post
<% end %>
